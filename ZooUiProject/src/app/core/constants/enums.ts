
export const enum toastrTitle {
    Error = 'Error',
    Success = 'Success',
    Warning = 'Warning'
}

export enum GENDER {
    MALE = 'Male',
    FEMALE = 'Female'
}

export enum Job {
    Worker = 'Worker',
    Manager = 'Manager',
    Vet = 'Vet'
}

export enum JobStatus {
    Created = 'Created',
    InProgress = 'InProgress',
    Finished = 'Finished'
}

export enum Role {
    Manager = 'Manager',
    Worker = 'Worker'
}
